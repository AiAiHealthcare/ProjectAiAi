# Security first
sudo: false

# Hatch latest pythons
language: python
python:
  - "3.5"
  - "nightly"
  - "pypy3"

# Batch install dependencies
install:
  - pip install -r requirements.txt
  - python setup.py

# Run unit tests
script: 
  - pytest
